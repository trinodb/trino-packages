<runtime>

    <!-- Bring server tar.gz over for the RPM -->
    <artifactSet to="/">
        <artifact id="io.trino:trino-server:tar.gz:${dep.trino.version}">
            <unpack useRoot="true" />
        </artifact>
    </artifactSet>

</runtime>
